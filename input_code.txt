#include <stdio.h>
extern int yylex();
extern char* yytext;

int main() {
    int token;
    while((token = yylex())) {
        printf("Token: %d \t Lexeme: %s\n", token, yytext);
    }
    return 0;
}